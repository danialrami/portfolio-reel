# uv / Python
.venv/
__pycache__/
*.egg-info/
*.pyc

# Environment
.env

# Rendered reels / captures (build artifacts)
reel/
*.mp4
*.mov
*.mkv

# The editing schema is intentionally named `schemas/reel/`; keep it tracked.
!schemas/
!schemas/capture/
!schemas/reel/
!schemas/reel/*.json

# Editor
.DS_Store
