"""reels: a LUFS-family screen-capture and reel primitive.

Two documents (capture.json, reel.json) plus the two contracts over them
(verify / prove), with a three-verdict posture: verified, violated,
unverifiable. See docs/SPEC.md.
"""

__version__ = "0.1.0"
