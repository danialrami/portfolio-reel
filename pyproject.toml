[project]
name = "portfolio-reel"
version = "0.1.0"
description = "Capture portfolio clips and compile demo reels"
requires-python = ">=3.12"
dependencies = [
    "inquirer>=3.1",
    "moviepy>=2.1",
    "pyyaml>=6.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
